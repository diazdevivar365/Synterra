{
  "$schema": "https://json.schemastore.org/tsconfig.json",
  "_note": "Forgentic Node-side tsconfig. Use for apps/api, apps/workers, and Node-only packages.",
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["ES2023"],
    "types": ["node"],
    "module": "ESNext",
    "moduleResolution": "Bundler"
  }
}
